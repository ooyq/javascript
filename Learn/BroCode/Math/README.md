```javascript
// let x = 3.14;
let x;
let y = 5;
let z = 9;
let maximum;
let minimum;

// x = Math.round(x); // 将数字四舍五入为最接近的整数
// x = Math.floor(x); // 将数字向下舍入为最接近的整数
// x = Math.ceil(x); // 将数字向上舍入到最接近的整数
// x = Math.pow(x,y); // 返回数字的y次方的值
// x = Math.sqrt(x); // 返回数字的平方根
// x = Math.abs(x); // 返回数字的绝对值

//maximum = Math.max(x,y,z);
//minimum = Math.min(x,y,z);

x = Math.PI;

console.log(x);
console.log(maximum);
console.log(minimum);
```
