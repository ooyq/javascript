```javascript
console.log('I like it!');
console.log("It's really good!");

window.alert("Warning!"); // 一般用于提示信息或者警告信息的展示
window.confirm("Really?"); // 用户点击“确定”或“取消”按钮时，弹窗会返回一个布尔值。确认返回true,取消返回false,由此可以判断用户的操作
window.prompt("What is your name?"); // 用户可以在弹框输入字符串，点击确认后，弹窗会返回输入框内的字符串

// This is a comment
/*
  This
  is
  a
  multiline
  commennt
*/
```
